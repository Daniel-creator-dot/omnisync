import React from 'react';
import GeneralLedger from './GeneralLedger';

// Accounts page wraps the General Ledger (Chart of Accounts + Journal Entries)
const Accounts = () => <GeneralLedger />;

export default Accounts;
